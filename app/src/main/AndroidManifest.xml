<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="bmt.android.com.bookmytractor"
    tools:ignore="MissingLeanbackLauncher">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.DashboardActivity"
            android:label="Dash Board">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.Splash.SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

        <activity
            android:name=".ui.UserPasswordsScreen.AddNewPasswordActivity"
            android:label="Add New Password" />
        <activity
            android:name=".ui.otpscreen.VerifyOtpActivity"
            android:label="Verify Otp" />

        <activity android:name=".ui.login.CreateAccountActivity"
            android:label="Create Account" />

        <activity android:name=".ui.login.LoginActivity"
            android:label="Login"/>

        <activity android:name=".ui.UserPasswordsScreen.ForgotPwdActivity"
            android:label="Forgot Password"></activity>

        <activity android:name=".ui.CropListActivity"
            android:label="Crops List"></activity>

        <activity android:name=".ui.MachineListActivity"
            android:label="Machines List"></activity>

        <activity android:name=".ui.Bookings.UserBookingsList"
            android:label="Your Bookings"></activity>
    </application>

</manifest>
